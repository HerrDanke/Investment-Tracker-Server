version: "3.8"

services:
  investment-tracker:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - RUST_LOG=info
      - DATA_DIR=/data
    restart: unless-stopped
